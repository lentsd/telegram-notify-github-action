{
  "scripts": {
    "build": "ncc build index.js -C -m -o dist"
  },
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/github": "^5.1.1",
    "@vercel/ncc": "^0.34.0",
    "node-telegram-bot-api": "^0.59.0"
  }
}
